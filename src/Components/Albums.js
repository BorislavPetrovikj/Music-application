import React from 'react';
import artists from "../db";
const Albums = (props) => {
     
    
    return (
        <div className="Albums">
            
        </div>
    );
};

export default Albums;